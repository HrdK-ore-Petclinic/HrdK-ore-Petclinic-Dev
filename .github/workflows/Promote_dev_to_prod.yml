name: Promote Image (Dev → Prod Tag)

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Prod version (e.g. v1.2.0)"
        required: true
      commit:
        description: "Optional: override commit (leave empty = latest)"
        required: false

jobs:
  promote:
    runs-on: ubuntu-latest

    env:
      SERVICES: |
        spring-petclinic-admin-server
        spring-petclinic-api-gateway
        spring-petclinic-config-server
        spring-petclinic-customers-service
        spring-petclinic-discovery-server
        spring-petclinic-genai-service
        spring-petclinic-vets-service
        spring-petclinic-visits-service

    steps:
      # ------------------------
      # CHECKOUT (needed for commit detection)
      # ------------------------
      - name: Checkout
        uses: actions/checkout@v4

      # ------------------------
      # DETERMINE COMMIT
      # ------------------------
      - name: Determine commit
        id: commit
        run: |
          if [ -z "${{ github.event.inputs.commit }}" ]; then
            COMMIT=$(git rev-parse --short HEAD)
            echo "No commit provided, using latest: $COMMIT"
          else
            COMMIT=${{ github.event.inputs.commit }}
            echo "Using provided commit: $COMMIT"
          fi

          echo "commit=$COMMIT" >> $GITHUB_OUTPUT

      # ------------------------
      # DOCKER LOGIN
      # ------------------------
      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # ------------------------
      # PROMOTE
      # ------------------------
      - name: Promote Images
        run: |
          COMMIT=${{ steps.commit.outputs.commit }}
          VERSION=${{ github.event.inputs.version }}

          for SVC in $SERVICES; do
            SRC=${{ secrets.DOCKER_USERNAME }}/$SVC:$COMMIT
            TARGET=${{ secrets.DOCKER_USERNAME }}/$SVC:$VERSION

            echo "Promoting $SVC ($COMMIT → $VERSION)"

            docker pull $SRC
            docker tag $SRC $TARGET
            docker push $TARGET
          done
